export class Project {
  public funds: number = 0
  constructor (public name: string, public managers: string, public description: string, public amount: string, public usage: string, public rewards: string, public type: string) { }
}
